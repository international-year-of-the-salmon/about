---
title: "Dataset registry"
---

```{r setup}
knitr::opts_chunk$set(include = FALSE, echo = FALSE, warning = FALSE, message = FALSE, error = FALSE)
library(ckanr)
library(tidyverse)
```

```{r, include = FALSE}
ckanr_setup(url = "http://iys.hakai.org")

pkgs <- package_list_current(as = "table")
pkgs <- pkgs %>% unnest(resources, name_repair = "universal")
names(pkgs)

subset <- pkgs %>% 
  select(title, num_resources, `metadata-point-of-contact`, description , url, notes)
```

```{r, include = TRUE}

# Check for DOI
# Check for minimum 2 resources with valid links
# Check for abstract
n_metadata_records <- length(pkgs)
```

```{html, include = TRUE}
<iframe src="https://graphcommons.com/graphs/471c7f04-98aa-42d1-9896-4a7313297ecf/embed" frameborder="0" style="overflow:hidden;border:1px solid #DDDDDD;width:1px;min-width:100%;height:400px;min-height:400px;" width="100%" height="400" allowfullscreen></iframe>
```


